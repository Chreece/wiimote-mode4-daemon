# wiimote-mode4-daemon
Linux daemon that converts Mayflash DolphinBar (Mode 4) Wiimote HID input into a virtual gamepad using uinput, with reliable connect/disconnect detection and full compatibility with RetroArch and Dolphin core.
